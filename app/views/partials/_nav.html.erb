<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1" style='border-top:none; box-shadow:none;'> -->
      <div class='navbar-header'>
        <a class="navbar-brand" href="/" style='background-color: #FFF; margin-left: -30px;'>
          <img src= <%= asset_path('logo.png') %> alt='Home' style='max-height: 50px;'>
        </a>
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#collapse-this">
       <span class="sr-only">Toggle navigation</span>
       <span class="icon-bar"></span>
       <span class="icon-bar"></span>
       <span class="icon-bar"></span>
      </button>
      </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="navbar-collapse collapse" id='collapse-this'>
      <ul class="nav navbar-nav">
        <% if !current_user.nil? %>
          <% if can? :read, Program %>
            <li><%= link_to "Programs", programs_url %></li>
          <% end %>
          <% if can? :read, Instructor %>  
            <li><%= link_to "Instructors", instructors_url %></li>
          <% end %>
          <% if can? :create, Child %>  
            <li><%= link_to "Children", new_child_path %></li>
          <% end %>
          <% if can? :manage, Program %>  
            <li><%= link_to "Dash", dash_path %></li>
          <% end %>
        <% end %>
      </ul>

      <% if logged_in? %>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <p id='nav-username' class='navbar-text'> 
            <%= current_user.username %> &nbsp
            <%= link_to '(Logout)', logout_path %>
          </p>
        </li>
      </ul>
      <% else %>
        <p class='navbar-text navbar-right'><%= link_to 'Login', login_path %></p>
      <% end %>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
