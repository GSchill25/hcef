
  <nav>
    <div class="nav-wrapper">
      <a href="/" class="brand-logo" id="pad-left"> HCEF</a>
      <ul class="right hide-on-med-and-down" id="pad-right">
        <li class="top-pad"><% if can? :create, Program %>
          <%= link_to "Add Program", new_program_path %><% end %></li>
        <li class="top-pad"><% if can? :read, Program %>
          <%= link_to "View Programs", programs_url %><% end %></li>
        <li class="top-pad"><% if can? :manage, Child %>
          <%= link_to "Add Child", new_child_path %><% end %></li>
        <li class="top-pad"><% if logged_in? %>
            <%= link_to 'Logout -', logout_path %><%= current_user.username %>
          <% else %>
            <%= link_to 'Login', login_path %>
          <% end %></li>
      </ul>
    </div>
  </nav>
