<p id="notice"><%= notice %></p>

<div class="container">
  <h4><%= @program.name %></h4>
  <div class="row">
    <div class="col l12 m12 s12">
      <table>
      <tr>
        <thead>
          <th>
            Name
          </th>
          <th>
            Program type
          </th>
          <th>
            Start date
          </th>
          <th>
            End date
          </th>
          <th>
            Attendance
          </th>
          <th>
            Location
          </th>
        </thead>
      </tr>
      <tr>
        <td><%= @program.name %></td>
        <td><%= @program.program_type == "after_school" ? "After School" : @program.program_type == "enrichment" ? "Enrichment" : "Field Trip" %></td>
        <td><%= @program.start_date %></td>
        <td><%= @program.end_date %></td>
        <td><%= @program.children.count %></td>
        <td><%= @program.location.nil? ? "None" : @program.location == "" ? "None" : @program.location.name %></td>
      </tr>
      </table>
    </div>
    </br></br>

    <div id="days">
      </br></br></br></br></br></br></br>
      <h5 align="center"> Daily Attendance/Time Allocation </h5>
      </br>
      <%= render :partial => "days" %>
    </div>

      <%= link_to 'New Day', new_after_school_path(program_id: @program.id) %> | 
      <%= link_to 'Edit Program', edit_program_path(@program) %> 
    <div class="col l6 m8 s12">
      </br></br></br></br>
      <u><h6>Children Present</h6></u>
      <ol>
        <% @program.children.each do |child| %>
          <li><%= child.name %></li>
        <% end %>
      </ol>
    </div> 

  </div>
  </br></br>
</div>
