<%= form_for(@school_district, validate: true) do |f| %>
  <% if @school_district.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@school_district.errors.count, "error") %> prohibited this school_district from being saved:</h2>

      <ul>
      <% @school_district.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
