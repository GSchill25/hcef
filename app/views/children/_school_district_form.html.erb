</br>
					<%= form_for(@school_district, :url=>{:controller=>"school_districts", :action=>"create"}) do |f| %>
							<div class="row">
								<div class="col-lg-6 col-md-6 col-sm-12">
									<label>Name</label><%= mark_required(@school_district, :name) %>
					        		<%= f.text_field :name %>
					        	</div>
						    </div></br></br>
						        
							<%= f.submit "Create" %>
					<% end %>